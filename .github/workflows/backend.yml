name: Backend CI

on: [pull_request]

jobs:
  lint:
    name: Lint (flake8)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install flake8

      - name: Run flake8
        run: flake8 backend/ --max-line-length=120 --exclude=backend/venv,backend/physics_engine

  test:
    name: Test (pytest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Install pytest
        run: pip install pytest

      - name: Run physics_engine tests
        run: pytest backend/physics_engine/tests/ -v

      - name: Run backend tests
        run: |
          cd backend
          PYTHONPATH=$GITHUB_WORKSPACE pytest tests/ -v
        continue-on-error: true  # remove this once you have backend 
        